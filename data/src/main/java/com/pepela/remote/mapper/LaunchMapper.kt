package com.pepela.remote.mapper

//class LaunchMapper(private val launchSiteMapper: LaunchSiteMapper)
//    : ModelMapper<LaunchModel, Launch> {
//
//    override fun from(model: LaunchModel): Launch {
//        val simpleDateFormat = SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ",
//                Locale.getDefault())
//        return Launch(model.flight_number, model.mission_name,
//                simpleDateFormat.parse(model.launch_date_local),
//                launchSiteMapper.from(model.launch_site), model.details)
//
//    }
//}
